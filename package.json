{
  "name": "unnos",
  "version": "1.0.0",
  "workspaces": [
    "web",
    "bot"
  ],
  "scripts": {
    "dev:web": "dotenv -- bun -F ./web dev",
    "start:web": "bun -F ./web start",
    "build:web": "bun -F ./web build",
    "dev:bot": "bun -F ./bot dev",
    "start:bot": "bun -F ./bot start"
  },
  "peerDependencies": {
    "typescript": "^5.8.2"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  }
}